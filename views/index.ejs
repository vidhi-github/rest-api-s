<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>all posts</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <div class="heading">
        <h1>Post Here</h1>
        <a href="http://localhost:8080/posts/new"><b>Create new post</b></a>
    </div>
    
    <% for(post of posts){ %>
    <div class="post">
        <h3 class="user">@&nbsp;&nbsp;<%= post.username %></h3>
        <h4 class="content"> <%= post.content %></h4>
        <a href="http://localhost:8080/posts/<%= post.id %>">See in detail</a>&nbsp;&nbsp;
        <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a><br/><br/>
        <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
            <button>Delete post</button><br/>
        </form>
    </div>
    <hr/>
    <% } %>
    <br/>
    
</body>
</html>